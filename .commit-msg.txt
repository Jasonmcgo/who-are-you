Bundle: V/O wiring + midpoint chart + Next Moves prose layer

Closes the chart-as-feedback-engine loop. User-journey vision now
end-to-end: release-grip → Movement bar grows on retake.

What landed:
- CC-100 V/O axis scaffolding (lib/victimOwnerAxis.ts)
- CC-101 V/O wiring: Aim boost on owners, Grip amplifier + Movement
  drag on victims, composes through GSAG spine
- CC-102 V/O calibration against real-person fixtures
- CC-103 trajectory chart anchored at midpoint (50,50) — movement
  visible from baseline
- CC-104 Next Moves prose layer: 3 registers (Load-Audit /
  Identity-Reframe / Build-Something), shape-aware routing
- 7 cohort-real fixtures (tests/fixtures/cohort-real/) extracted
  from saved prod sessions; architecturally distinct from synthetic
  shape-pattern diagnostic fixtures
- CODEX cleanups: audit expectation rebaselines, mass cache regen
  (synthesis3 → proseRewrites → others), handsCard audit extractor
  hardened, twoTier baseline regenerated
- proseRewriteLlm prompt: shape-aware vocabulary constraints added
  for cindyType / danielType (prevents architect-coded language
  leaking into caregiver / steward Lens prose)

Audit: 77/77 green. Engine math byte-identical for canonical Jason
fixture (Goal=92, Soul=59, Aim=64.9, Grip=26.3, Movement=77.28).
tsc --noEmit clean. Lint clean (1 pre-existing warning preserved).

Empirical wins surfaced during the bundle:
- Kevin tension-fire (measured 87 anchored vs predicted 50 balanced)
  validates Option C canon: trust data, surface divergence as the
  finding
- Compressed-owner pattern (Harry V/O 60, Michele V/O 36): owner
  trait + state pressure produces V/O mid-band reading, routes to
  Load-Audit register
- Cache-regen ordering canon: synthesis3 must run before
  proseRewrites (Path section embeds synthesis3 output)
- CODEX-LLM-* editorial-review canon: mechanical "ran clean" is
  insufficient when scripts write LLM content; explicit register
  and hedge review required before accept

Cost: ~$5-7 LLM (mass cache regen + path re-run after order fix +
Cindy Lens shape-aware re-roll).

Open follow-ups queued (do not block this push):
- CC-VO-MICHELE-REVALIDATION (real-world: Michele retakes on
  post-CC-097A instrument)
- CC-VO-VERB-DISAMBIGUATION (speculative, hold for cohort signal)
- Kevin/Cindy expressed-vs-lived band canon decision
- CC-PROSE-SHAPE-GUARDRAILS-V2 (broader prompt hardening across
  non-architect shapes for all sections)
