# CODEX-074 — React UI: Disposition Signal Mix Section Sync

**Scope frame:** Companion fix to CODEX-073, post-CC-072. CC-072 lands the new OCEAN architecture in `lib/ocean.ts` and the markdown render in `lib/renderMirror.ts` — independent intensities (no 100%-summing), Openness subdimensions, Agreeableness register, Emotional Reactivity 0% guard, OCEAN ↔ Goal/Soul cross-references, SVG bar chart. **The React UI (`app/components/InnerConstitutionPage.tsx`) currently renders the OLD Disposition Map JSX block with the 100%-summing distribution that this whole chain is designed to replace.** This CODEX swaps the old block for the new Disposition Signal Mix shape.

**Per `feedback_codex_vs_cc_prompt_naming` memory:** mechanical/surgical scope. Single file, pattern-mirror against CC-072's markdown output, no architectural decisions.

**Pre-condition:** CC-072 must have landed and been committed before this CODEX fires. The new OCEAN data shape (`OceanOutput.intensities`, `OceanOutput.dominance`, `OceanOutput.opennessSubdimensions`, etc.) and the SVG renderer (`lib/oceanDashboard.ts:renderOceanDashboardSVG`) must exist when this CODEX starts.

---

## Launch Directive

Run with `claude --dangerously-skip-permissions` or `/permissions` → bypass.

## Execution Directive

Single pass. The markdown render path in `lib/renderMirror.ts` (post-CC-072) is the source of truth for what data should display and in what order. The React path mirrors it. **Verify against CC-072's actual output before writing JSX** — the spec describes the intended shape, but CC-072 may have made minor naming choices (e.g., `intensities` vs `traits`, exact dashboard field names). Match what landed, not what the spec predicted.

## Bash Commands Authorized

- `npx tsc --noEmit`
- `npm run lint`
- `npm run dev` — live verification in browser
- `git status`, `git diff`
- `cat`, `grep`

## Read First (Required)

1. `app/components/InnerConstitutionPage.tsx` — full file. Find the existing Disposition Map block (around line ~292, conditional on `constitution.ocean`). That block is the replacement target.
2. `lib/renderMirror.ts` (post-CC-072) — find the new lines that emit `## Disposition Signal Mix`, the disclaimer, lede paragraph, per-trait paragraphs, SVG embed, optional expander payload. Note the exact data fields read from `constitution.ocean`.
3. `lib/ocean.ts` (post-CC-072) — for the OceanOutput shape and the per-trait intensity / dominance / subdimension fields actually exported.
4. `lib/oceanDashboard.ts` (post-CC-072, NEW FILE) — `renderOceanDashboardSVG(...)` returns the SVG string the React component embeds.
5. `lib/types.ts` (post-CC-072) — `OceanOutput`, `OceanIntensity`, `OceanDominance`, `OceanSubdimensions`, `DispositionSignalMix`. The exact shapes the React component reads.
6. `docs/ocean-disposition-spec.md` — esp §1 hierarchy, §2 math/label canon, §3 Openness subdimensions, §4 Agreeableness register, §5 Emotional Reactivity confidence, §6 render register, §10 guardrails.
7. `prompts/completed/CC-072-…md` (after it moves to completed) — the canon source for what landed and what cross-references to render.
8. `prompts/completed/CODEX-073-…md` (sibling pattern) — for the JSX shape used to add Closing Read and Movement; this CODEX mirrors that shape for OCEAN.

## Allowed to Modify

This file only:

1. **`app/components/InnerConstitutionPage.tsx`** — replace the existing Disposition Map JSX block with a new Disposition Signal Mix block. Mirror the order and conditional-gating pattern that CODEX-073 used for Closing Read and Movement. The replacement is structural — old block out, new block in — not an addition alongside.

## Out of Scope (Do Not)

1. **Do NOT modify `lib/renderMirror.ts`** or any engine file. CC-072 already shipped those changes; this CODEX surfaces them in React.
2. **Do NOT modify `lib/types.ts`, `lib/ocean.ts`, `lib/oceanDashboard.ts`** or any other engine file. The data shape is set by CC-072.
3. **Do NOT add new component files.** The new Disposition Signal Mix render is inline JSX in `InnerConstitutionPage.tsx`, matching the existing Disposition Map block's location and the CODEX-073 pattern for Closing Read/Movement.
4. **Do NOT change section ordering.** The Disposition Signal Mix block goes exactly where the old Disposition Map block was — between Movement and Work Map. Nothing else moves.
5. **Do NOT use a charting library.** The SVG is already generated by `lib/oceanDashboard.ts:renderOceanDashboardSVG()`. Embed via `dangerouslySetInnerHTML` (safe — internal-generated, no user input).
6. **Do NOT keep the old Disposition Map JSX block in any form.** It produces the 100%-summing distribution that's a known bug. Old block out completely.
7. **Do NOT render the 100%-summing distribution percentages anywhere.** The new render uses independent per-trait intensities (0–100 each, no normalization across).
8. **Do NOT render "Emotional Reactivity 0%" anywhere.** When the trait is under-detected, the render shows "low or under-detected" with proxy-signal disclosure per OCEAN spec §5.1.
9. **Do NOT use the section heading "OCEAN", "Big Five", or "Personality"** — heading is "Disposition Signal Mix" per OCEAN spec §6.1.
10. **Do NOT add new dependencies.**
11. **Do NOT touch `app/page.tsx`** or any other route file.
12. **Do NOT modify the audit, the spec memo, or any test file.**
13. **Do NOT use therapy-coded register** ("your inner work", "shadow integration", "authentic self", "your true self") per OCEAN spec §6.4.
14. **Do NOT collapse Agreeableness signals into "you are agreeable"** without the loyalty/service register disambiguation per OCEAN spec §4.

## Acceptance Criteria

### Section identity and placement

1. The existing Disposition Map JSX block in `InnerConstitutionPage.tsx` is fully replaced. No vestigial elements remain — no old heading, no old distribution sentence, no old per-trait paragraphs from the prior block.
2. The new section heading is **"Disposition Signal Mix"** (matching markdown `## Disposition Signal Mix`). Verified by grep: no occurrence of `Disposition Map` heading text in `InnerConstitutionPage.tsx`.
3. The new section sits in the same position the old block occupied — after the Movement section (which CODEX-073 added) and before Work Map.
4. The section uses the same outer `<section>` wrapper, heading style, and spacing as sibling sections (Closing Read, Movement, Work Map, Love Map). Visual consistency with siblings is required.
5. Section is conditionally gated: renders only when `constitution.ocean` is present and `constitution.ocean.intensities` (or whatever the post-CC-072 field is) is non-null.

### Disclaimer

6. The disclaimer line appears immediately under the heading per OCEAN spec §2.4: *"These describe the relative weight of disposition signals detected by the instrument across this assessment. They are not percentile scores against a population, and the trait readings are independent — high in one does not mean low in another."* Use the exact text CC-072 emits in markdown (verify against `lib/renderMirror.ts`).

### Lede paragraph

7. The lede paragraph names the strongest signal in narrative-name-first form per OCEAN spec §6.2 (e.g., *"Your strongest signal is in how you organize your effort — Big Five Conscientiousness — registering as moderate-high."*). Use the exact text CC-072 produces. Render as `<p>`.

### Per-trait paragraphs

8. Per-trait paragraphs render in dominance rank order (per `OceanOutput.dominance`), one paragraph per trait. Each paragraph mirrors the markdown render exactly — narrative name + Big Five trait name + intensity band + register interpretation + cross-reference to Goal/Soul where corroborating composite is present.
9. Render bands appear as words (under-detected / low / moderate / moderate-high / high) per OCEAN spec §2.1. Numeric intensities do NOT appear in user-facing prose paragraphs — band names only.
10. Cross-reference sentences fire per OCEAN spec §7 when the corroborating composite is present (Conscientiousness × Goal, Openness × Soul, Architectural Openness × Give synthesis, Extraversion × Soul visibility, Agreeableness × Soul interpretation, Emotional Reactivity × pressure response).

### Openness subdimension flavor sentence

11. The Openness paragraph leads with the one-sentence flavor read per OCEAN spec §3.5 (intellectual-led / aesthetic-led / novelty-led / architectural-led / mixed). The flavor sentence is the lead; the per-subdimension breakdown is data on `OceanOutput.opennessSubdimensions` for an optional UI expander.
12. (Optional, if visual layout supports it) An expander/collapsible reveals the four subdimension intensities in band form. If the existing `InnerConstitutionPage.tsx` doesn't have an expander pattern, omit this — render only the flavor sentence and let the data carry forward to a future UI CC.

### Agreeableness register

13. When `OceanOutput.intensities.agreeableness ≥ 60`, the Agreeableness paragraph explicitly disambiguates loyalty/service/moral-concern from accommodation. Match CC-072's markdown text exactly.
14. The standalone phrase *"You are highly agreeable"* without the disambiguation does NOT appear. Verified by grep.

### Emotional Reactivity confidence

15. When the trait is under-detected (intensity 0 OR `proxyOnly: true` OR signal density below threshold), the paragraph translates to "low or under-detected" with the proxy-signal disclosure per OCEAN spec §5.2. Match CC-072's markdown text exactly.
16. The string "Emotional Reactivity 0%" does NOT appear in any user-facing render. Verified by grep.

### SVG bar chart

17. The SVG horizontal bar chart from `lib/oceanDashboard.ts:renderOceanDashboardSVG(constitution.ocean)` (or whatever the post-CC-072 signature is) renders inline via `dangerouslySetInnerHTML={{ __html: svgString }}` inside a wrapper `<div>` with appropriate styling (max-width, aspect-ratio matching the SVG's intent).
18. The SVG renders only when the data is present; it does not appear as an empty box if intensities are missing. Conditional gating is independent from the prose gating — if intensities exist but prose doesn't, SVG still renders.

### Visual consistency and Markdown parity

19. After CC-067-068-070-071-072, CODEX-073, CODEX-074: the on-page report section order is exactly:
    - Mirror / A Synthesis
    - Closing Read
    - Movement
    - **Disposition Signal Mix** (this CODEX)
    - Work Map
    - Love Map
    - Map (eight cards)
    - Growth Path / Conflict Translation / Mirror-Types Seed
    - Confirmed/Open Tensions
20. Markdown export and React UI contain the same canonical sections in the same order, even if visual formatting differs. Verified by comparing `## ` headings in markdown output to JSX heading list.

### Build and runtime

21. `npx tsc --noEmit` exits 0.
22. `npm run lint` exits 0.
23. `npm run dev` renders the report with the new Disposition Signal Mix block in correct position, no 100%-summing percentages anywhere, no "0%" for Emotional Reactivity, intensity band words present (under-detected/low/moderate/moderate-high/high). Verify by browser inspection on at least 2 fixture-shaped sessions if possible.
24. `git status --short` shows ONLY `M app/components/InnerConstitutionPage.tsx`.

## Report Back

1. **Summary** — what was replaced in 3–5 sentences.
2. **Old block diff** — paste the old Disposition Map JSX that was removed (verbatim).
3. **New block** — paste the inserted Disposition Signal Mix JSX (verbatim).
4. **Verification** — describe the on-page render after `npm run dev`. Confirm the new block renders correctly. Quote the rendered headings, the disclaimer, and one per-trait paragraph as proof of parity with the markdown render.
5. **Markdown vs React parity check** — dump the markdown for one fixture (post-CC-072) and confirm the same `## ` headings appear in both surfaces.
6. **Verbatim text source** — note whether the per-trait paragraph text was lifted from `lib/renderMirror.ts` directly (preferred), composed via a shared helper, or re-emitted in JSX. Match what CC-072 made available.
7. **Out-of-scope verification** — `git status --short` confirming only `InnerConstitutionPage.tsx` modified.
8. **Open questions** — any rendering edge cases (SVG sizing on mobile, dark-mode contrast on bar chart, intensity band word-wrap) that surfaced.

---

## Method note

This CODEX is the React-side completion of CC-072. CC-072 fixed the markdown render and the engine math. The React UI was the missing surface. Once CODEX-073 (Closing Read + Movement) and this CODEX-074 (Disposition Signal Mix) both ship, the markdown and React paths are in full parity for the post-revision Goal/Soul/Give + OCEAN architecture.

The data flow is already correct after CC-072. The new fields exist on `constitution.ocean` and the new SVG renderer is exported from `lib/oceanDashboard.ts`. This CODEX surfaces what's already there — same pattern as CODEX-073 made for Closing Read and Movement.

If during implementation you find that CC-072 chose different field names than the spec predicted (e.g., `traits` instead of `intensities`, or different SVG renderer signature), match the actual landed shape — do NOT modify CC-072's output to fit the spec's predicted names. The spec is authoritative for canon; CC-072's code is authoritative for field naming.

If you find that `lib/renderMirror.ts` produces text via a shared helper that this CODEX could reuse instead of re-emitting the same text in JSX, prefer the shared helper to avoid drift. Two surfaces, one source of truth for the prose.
